<UserControl x:Class="UserControlExample.CircularProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControlExample"
             
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="150">
    <UserControl.Resources>
        <local:ProgressToColorConverter x:Key="ProgressToColorConverter"/>
    </UserControl.Resources>
    <Viewbox>
        <Grid Width="100" Height="100">
            <!-- Фон круга -->
            <Ellipse Stroke="LightGray" StrokeThickness="5" Fill="Transparent"/>

            <!-- Прогресс -->
            <Path  
                  StrokeThickness="5" 
                  StrokeStartLineCap="Round" 
                  StrokeEndLineCap="Round">
                <Path.Stroke>
                    <SolidColorBrush Color="{Binding ProgressPercentage, 
                                            Converter={StaticResource ProgressToColorConverter},
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Path.Stroke>

                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="50,5">
                            <ArcSegment x:Name="progressArc"
                                        Point="50,5" 
                                        Size="45,45" 
                                        SweepDirection="Clockwise"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
                <!--<Path.RenderTransform>
                    <RotateTransform Angle="{Binding ProgressAngle, RelativeSource={RelativeSource AncestorType=local:CircularProgressBar}}"/>
                </Path.RenderTransform>-->
            </Path>

            <!-- Текст с процентом -->
            <TextBlock Text="{Binding PercentageText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
        </Grid>
    </Viewbox>
</UserControl>
