<Window x:Class="Task2_9_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task2_9_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Window.CommandBindings>
        <CommandBinding Command="local:CustomCommands.ChangeColor" Executed="ChangeColorExecuted"/>
        <CommandBinding Command="ApplicationCommands.Undo" Executed="UndoExecuted"/>

    </Window.CommandBindings>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Добавить">
                <MenuItem Header="Изменить цвет" 
                          Command="local:CustomCommands.ChangeColor"/>
                <MenuItem Header="Отменить" 
                          Command="ApplicationCommands.Undo"/>
            </MenuItem>

        </Menu>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition  Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBox  Text="Текущий цвет"
                      HorizontalAlignment="Center"
                      Grid.Row="0"
                      Grid.Column="0"/>
            <TextBox x:Name="Dock" 
                     Grid.Row="1"
                     Grid.Column="0"/>

            <Button  Content="Изменить цвет" 
                    HorizontalAlignment="Center" 
                     VerticalAlignment="Center"
                    Grid.Row="1"
                    Grid.Column="1"
                     Command="local:CustomCommands.ChangeColor"
                     Width="100" Height="30"/>
            <Button  Content="Отменить" 
                    HorizontalAlignment="Center" 
                     VerticalAlignment="Center"
                      
                     Command="ApplicationCommands.Undo"
                    Grid.Row="3"
                     Grid.Column="1"
                    Width="100" Height="30"/>
            <TextBox  Text="Предыдущий цвет"
                      HorizontalAlignment="Center"
                      Grid.Row="2"
                      Grid.Column="0" />

            <TextBox x:Name="UndoDock" Grid.Row="3"
                     Grid.Column="0" />

        </Grid>
    </DockPanel>

</Window>
