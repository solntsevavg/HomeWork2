<Window x:Class="Task2_6_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task2_6_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:TextLengthConverter x:Key="TextLengthConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- текстовое поле (TextBox) для ввода произвольного текста -->
        <TextBox x:Name="InputTextBox" 
             FontSize="20"
             Grid.Row="0"/>

        <!--флажок (CheckBox) с подписью "Показать детали"-->
        <CheckBox x:Name="checkBox" 
                  Grid.Row="1"
                  Margin="0,15"
                  Content="Показать детали"/>

        <!-- Текущая длина текста -->
        <TextBlock x:Name="OutTextBox"
        Grid.Row="2"
               Margin="0,15"
                FontSize="20"
                Visibility="{Binding ElementName=checkBox, Path=IsChecked,
                              Converter={StaticResource BoolToVisibilityConverter}}"
               
               Text="{Binding ElementName=InputTextBox, 
                     Path=Text.Length, StringFormat='Длина текста: {0}'}"/>

        <!--Визуальный индикатор заполнения (ProgressBar), где:
        Максимальное значение = 100-->
        <ProgressBar Grid.Row="3"
                     Height="25" 
                     Maximum="100"
                 Visibility="{Binding ElementName=checkBox, Path=IsChecked,
                             Converter={StaticResource BoolToVisibilityConverter}}"
                 Value="{Binding ElementName=InputTextBox, Path=Text, 
                 Converter = {StaticResource TextLengthConverter}}"/>

    </Grid>
</Window>
