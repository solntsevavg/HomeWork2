<Window x:Class="Task2_8_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task2_8_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="700">
    <Window.Resources>
        <DataTemplate x:Key="ProductDataTemplate">
            <Border x:Name="border" Padding="10" BorderBrush="Green" BorderThickness="1" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" 
                           Width="100" Height="100"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                            Source="{Binding ImageWay}" />
                    <TextBlock x:Name="Name"
                               Grid.Row="0" Grid.Column="1" 
                               Margin="10,5"
                               FontWeight="Bold"
                               Text="{Binding ProductName}" />
                    <TextBlock Grid.Row="1" Grid.Column="1" 
                               Margin="10,5"
                               FontWeight="Bold"
                               Text="{Binding Cost, StringFormat='{}{0:N2} ₽'}"/>
                </Grid>
                <!--Можно было вот так сделать:

                <TextBlock Text="{Binding ProductPrice, StringFormat={}{0:C}, ConverterCulture=ru-RU}"/>-->
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding ProductType}" Value="Food">
                    <Setter TargetName="border" Property="BorderBrush" Value="Gray" />
                    <Setter TargetName="border" Property="Background" Value="#FFB8FEB8" />
                    <Setter TargetName="Name" Property="Foreground" Value="Green" />

                </DataTrigger>
                <DataTrigger Binding="{Binding ProductType}" Value="Technic">
                    <Setter TargetName="border" Property="BorderBrush" Value="Gray" />
                    <Setter TargetName="border" Property="Background" Value="#FFC5ECF9" />
                    <Setter TargetName="Name" Property="Foreground" Value="Blue" />
                </DataTrigger>

            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    
    <StackPanel>
        
        <TextBlock Text="Список товаров" 
               FontSize="16" 
               FontWeight="Bold"
                HorizontalAlignment="Center"
 VerticalAlignment="Center"/>
        <ListBox Name="lstBox" 
                 Width="400" 
                 Margin="10" 
                 
                 HorizontalContentAlignment="Stretch"
ItemTemplate="{StaticResource ProductDataTemplate}" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

    </StackPanel>
    
</Window>
